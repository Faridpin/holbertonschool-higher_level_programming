#!/usr/bin/python3
def uppercase(str):
    result=""
    for i in (str):
        if "a" <= i <= z:
            result += chr(ord(char)-32)
        else:
            result += char
            print("{}".format(result))
